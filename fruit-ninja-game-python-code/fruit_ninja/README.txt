Fruit Ninja - Setup

1. Install pygame:
   pip install pygame

2. Run:
   python fruit_ninja.py

Folder has: fruit_ninja.py, back.jpg, comic.ttf, images/ (fruit + bomb + lives + explosion pngs, auto-generated placeholders).

Click/slide mouse over fruit to slice. Hit bomb = lose life. 3 lives = game over.
